apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: topology-volume-immediate
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: nvmesh-concatenated