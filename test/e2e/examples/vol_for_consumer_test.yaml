apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nvmesh-vol-for-consumer-test
  namespace: nvmesh-csi
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 3Gi
  storageClassName: nvmesh-raid0