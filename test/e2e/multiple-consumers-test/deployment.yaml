---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nvmesh-vol-for-consumer-test
  namespace: nvmesh-csi
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 3Gi
  storageClassName: nvmesh-raid0
---
kind: DaemonSet
apiVersion: apps/v1
metadata:
  name: multi-consumer-test
  namespace: nvmesh-csi
spec:
  selector:
    matchLabels:
      app: nvmesh-csi-testing
  template:
    metadata:
      labels:
        app: nvmesh-csi-testing
    spec:
      containers:
      - name: consumer1
        image: multi-consumer-test:develop
        env:
          - name: WORKER_ID
            value: "consumer1"
        volumeMounts:
          - name: volume-mnt
            mountPath: /mnt/vol/
      - name: consumer2
        image: multi-consumer-test:develop
        env:
          - name: WORKER_ID
            value: "consumer2"
        volumeMounts:
          - name: volume-mnt
            mountPath: /mnt/vol/
      volumes:
        - name: volume-mnt
          persistentVolumeClaim:
            claimName: nvmesh-vol-for-consumer-test