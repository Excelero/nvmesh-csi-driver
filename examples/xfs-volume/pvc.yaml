apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vol-xfs
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: nvmesh-xfs
